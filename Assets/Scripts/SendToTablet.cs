using UnityEngine;
using System.Diagnostics;
using System;
namespace DefaultNamespace
{
    public class SendToTablet:MonoBehaviour
    {
        void Update()
        {
            if (Input.GetKeyDown("[1]"))
            {
                // Envoie un "tap" sur la tablette à la position (500, 1000)
              
            }
        }
    }
}